[package]
name = "crust-syscall"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0"

[lib]
name = "crust_syscall"

[dependencies]
# No dependencies - pure no_std syscall wrappers

[features]
default = ["std"]
std = []  # Feature flag for std support in main binary
